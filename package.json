{
  "name": "livedevicetrack",
  "version": "1.0.0",
  "description": "Live device tracking app",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"node app.js --watch",
    "db:sync:init": "npx prisma migrate dev --name init",
    "db:sync": "prisma migrate dev --name migration"
  },
  "author": "Rakesh Mondal",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.3.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "prisma": "^6.3.1",
    "socket.io": "^4.8.1"
  }
}
